# Legacy: SUM(−)LOK

This folder preserves the original SUM(−)LOK number-puzzle prototype that
previously lived at the repository root. It has been moved here, unmodified,
to make room for **The Deceivers** social deduction card game, which is now
built at the repository root.

Open `sum-lok-index.html` directly in a browser to play the original puzzle.
